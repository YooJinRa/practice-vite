import React from 'react';
import Button from '@components/atoms/Button/Button';
import Panel from '@components/molecules/Panel/Panel';

const Form = () => {
  return (
    <Panel title="CONTEXT API PRACTICE">
      <Button>Sign In</Button>
      <Button>Sign Up</Button>
    </Panel>
  );
};

export default Form;
